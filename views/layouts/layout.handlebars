<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <!--Importación de la librería de Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700;900&display=swap" 
    rel="stylesheet">
    <!--Importación de la carpeta css (public) -->
    <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <!--Creación titulo a partir del paso de variable (Controller) -->
    <title>{{nombrePagina}}</title>
</head>
<body>
    <!--Cascarón inicial -->
    <div class="nombre-sitio contenedor">
        <h1>
            <a href="/">Plataforma Trabajos</a>
        </h1>
        <!--Recibe el dato del controlador modo true, si no existe no renderiza -->
        {{#if barra}}
            <div class="buscador">
                <form action="#">
                    <input type="text" class="buscar">
                    <input type="submit" class="Buscar">
                </form>
            </div>
        {{/if}}
        {{#if cerrarSesion}}
            <div class="cerrar-sesion">
                <p>Hola! {{nombre}}</p>
                {{#if imagen}}
                    <img src="/uploads/perfiles/{{imagen}}" class="admin-perfil">
                {{/if}}
                <a href="/cerrar-sesion">Cerrar Sesión</a>
                <a href="/administracion">Administración</a>
            </div>
        {{/if}}
    </div>
    <!--Espacio para inyectar nuevas páginas -->
        

    <header class="site-header contenedor separador">
        <h2>{{nombrePagina}}</h2>
        {{#if tagline}}
            <p class="tagline">{{tagline}}</p>
        {{/if}}
        {{#if boton}}
            <a href="/vacantes/nueva" class="btn btn-azul">
                Publica Nuevas Vacantes
            </a>
        {{/if}}
    </header>

    <div class="contenedor-principal contenedor">
        {{#if mensajes}}
            <div class="alertas">
                {{#mostrarAlertas mensajes}}
                {{/mostrarAlertas}}
            </div>
        {{/if}}
        {{{body}}}
    </div>
    
    <script src="/js/trix.js"></script>
    <script src="/dist/bundle.js"></script>
</body>
</html>